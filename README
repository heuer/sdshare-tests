==================
SDShare test suite
==================

This project provides a test suite for SDShare implementations.

Information about SDShare provides http://www.egovpt.org/fg/CWA_Part_1b.

----------------------
Running the test suite
----------------------
The system property "org.isotopicmaps.sdshare.serveraddress" must 
be set to an IRI where the SDShare server runs (i.e. http://localhost:8888/).

Example:

    java -Dorg.isotopicmaps.sdshare.serveraddress=http://localhost:8888 -jar sdshare-tests-0.1.2-SNAPSHOT.jar
